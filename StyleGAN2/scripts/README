1) ortho 1 layers + training partial parameters + without loading d
   low-quality, but acceptable disentangling results
2) ortho 1 layers + training partial parameters + with loading d
   worse disengtanglement.
3) ortho 1 layers + training all parameters + with loading d
   10000 steps from low-quality to high-quality output, acceptable disentangling results

4) ortho all layers + training partial parameters + with loading d

5) ortho all layers + training partial parameters + without loading d

6) ortho all layers + training all parameters + with loading d





